[English](RELEASE_NOTES.md) | 中文

# RiscDom v0.9.1

> **这是修复版，与 v0.9.0 同样的两条 caveat。** **macOS 与 Linux 的包由 CI 构建，从未在真实机器上
> 启动过**，而且它们**未签名**（macOS 的 Gatekeeper 会拦下首次运行；Windows 的 SmartScreen 会对安装包
> 告警）。并且 **Windows 仍是黄金路径被验证过的平台**：由别人在干净机器上走一遍这件事，仍未发生。

**v0.9.1 一句话：** 它修掉了唯一让 v0.9.0 桌面版完全不可用的那一件事 —— 打开即停在一个怎么输都过不去
的登录页。

## 本版修复

- **桌面版停在登录页**（v0.9.0）。前门对**所有**运行时都索要 token，找不到就画出登录页 —— 但桌面端
  根本没有 token：它从不与控制平面通话，因为它的宿主就跑在同一个进程里。它让你输入的 token 也永远不可能
  被接受，因为校验 token 会去请求一个桌面端根本没有的 `/v0/health`。现在**在任何 token 被读取之前**就
  先回答桌面端，而 token 门住在一个桌面端永不进入的独立组件里。已在 Windows 真机验证：外壳直接打开、
  每个设置 tab 都能切、聊天与串口面板俱在、审计页列出整条链（223 条事件，完整），外观页的语言与主题改动
  即时生效。

## 本版未变

其余与 v0.9.0 完全一致：控制平面与它的 CLI、沙箱里的 Zig 与 Rust、管理程序与它的 Web 看板、以及多 Agent
接口。**已知限制就是 v0.9.0 那次发布列出的那些**，此处不再重述 —— 见
<https://github.com/breakevery/riscdom/releases/tag/v0.9.0>。安装包形态与那 6 个附件相同，只是版本号改成
`0.9.1`。

## 我们要去哪里

**连出去** —— v1.0 会让桌面应用连上一台内网的 RiscDom 服务器，于是一块看板可以统管多个节点。（今天
桌面应用跑的是它自己内嵌的那个节点。）

**服务进来** —— v1.0 也会让桌面应用充当本机的管理宿主，把同一块只读看板暴露给局域网上的手机和其它
设备。（今天那块看板由 `riscdom-server --web-root` 提供。）

## 安全

本项目**不分发**任何 API key：所有模型访问都是自带 key。key 从不离开你的机器，审计日志住在 AI 工作区
之外且只可追加，任何东西都不会被上传。控制平面默认只绑回环，且除非以 `--no-auth` 启动，都要求 token；
浏览器看板从数据目录读同一个 token。QEMU 与 RISC-V 工具链是各自许可证下的独立程序；见
[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。完整声明与上报流程在 [SECURITY.md](SECURITY.md)。

## 许可证

[Apache License 2.0](LICENSE)。贡献需要 [CLA](CLA.md) —— 见 [CONTRIBUTING.md](CONTRIBUTING.md)。
